import 'package:flutter/material.dart';

import 'app.dart';

void main() {
  // Keep the executable entry point small; application composition lives in
  // app.dart so pages and reusable widgets can evolve independently.
  runApp(const ThuliumApp());
}
